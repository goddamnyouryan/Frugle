<% current_user.categories.each do |category| %>
	<li class="follwing_col2">
		<%= category.title %> <%= link_to "(x)", categorizations_unfollow_path(:category_id => category.id), :remote => true %>
	</li>
<% end %>