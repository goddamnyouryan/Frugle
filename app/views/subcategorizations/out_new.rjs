page.replace_html "subcategory_#{@subcategory.id}", "#{link_to "#{@subcategory.title}", out_destroy_subcategorization_path(:subcategory_id => @subcategory.id), :remote => true, :style => "background-color:yellow;"}"
page.replace_html "frugles", :partial => 'neighborhoods/frugle', :collection => @results, :as => :frugle
page << @map.clear_overlays
for marker in @markers
  page << @map.add_overlay(marker)
end